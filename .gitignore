# Maven
target/
*.class
*.jar
*.war

# IDE
.idea/
*.iml
.vscode/
*.swp

# DevSecOps: NEVER commit secrets
.env
*.env
application-prod.properties
application-staging.properties

# Terraform state — contains real infrastructure data and possibly secrets
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/.terraform.lock.hcl

# detect-secrets baseline — commit this (it records known-false-positives)
# .secrets.baseline  ← intentionally NOT ignored; should be committed

# OS
.DS_Store
Thumbs.db
/bin/
